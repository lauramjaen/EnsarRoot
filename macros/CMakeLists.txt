GENERATE_ROOT_TEST_SCRIPT(${ENSARROOT_SOURCE_DIR}/macros/runsim.C)
add_test(runsim ${ENSARROOT_BINARY_DIR}/macros/runsim.sh)
SET_TESTS_PROPERTIES(runsim PROPERTIES TIMEOUT "100")
SET_TESTS_PROPERTIES(runsim PROPERTIES PASS_REGULAR_EXPRESSION "TestPassed;All ok")
